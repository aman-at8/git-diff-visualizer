# Server
PORT=3001
NODE_ENV=development

# Logging level: error | warn | info | debug
# debug  – verbose, shows every clone/compare step (default in dev)
# info   – operational events only (default in prod)
# warn   – only warnings and errors
# error  – silent except on failures
LOG_LEVEL=debug
