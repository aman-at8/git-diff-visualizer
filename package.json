{
  "name": "git-diff-visualizer",
  "version": "0.1.0",
  "description": "Side-by-side visual diff tool for comparing files across two git repositories at any branch",
  "keywords": ["git", "diff", "visualizer", "code-review", "cherry-pick"],
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
